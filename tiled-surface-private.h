
#include "brushmodes.h"

void render_dab_mask (uint16_t * mask,
                        DabBounds *bb,
                        float x, float y,
                        float radius,
                        float hardness,
                        float aspect_ratio, float angle
                        );
